<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Login - <%= webData.webName %></title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="/main.css">
    </head>

    <body class="bg-light">
        <div class="text-center my-3">
            <a href="/" class="navbar-brand fs-2 text-primary fw-bold"><%= webData.webName %></a>
        </div>

        <!-- Sign in Card -->
        <div class="fullscreen-center">
            <div class="card">
            <!-- Display the error message as a popup if there is an existing user -->
                <% if (error) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <%= error %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
                <div class="text-center mb-4">
                    <h5>Sign In</h5>
                </div>
                <form action="/auth/login" method="POST">
                    <div class="mb-3">
                        <label for="username" class="form-label">Username:</label>
                        <input type="text" id="username" name="username" class="form-control" placeholder="Enter your username" required>
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password:</label>
                        <input type="password" id="password" name="password" class="form-control" placeholder="Enter your password" required>
                    </div>
                    <button type="submit" class="btn btn-warning w-100 text-white">Continue</button>
                </form>
                <hr>
                <div class="text-center">
                    <p class="mb1">New to eCommerce Web?</p>
                    <a href="/auth/register" class="btn btn-secondary w-100">Create an Account</a>
                </div>
            </div>
        </div>

        <%- include('partials/footer') %>
    </body>
</html>
