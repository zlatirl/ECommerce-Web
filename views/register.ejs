<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Register - <%= webData.webName %></title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="/main.css">

        <!-- hCaptcha API -->
        <script src="https://hcaptcha.com/1/api.js" async defer></script>
    </head>

    <body class="bg-light">
        <div class="text-center my-5">
            <a href="/" class="navbar-brand fs-2 text-primary fw-bold"><%= webData.webName %></a>
        </div>
        <div class="fullscreen-center">
            <div class="card">
                <!-- Display the error message as a popup if there is an existing user -->
                <% if (error) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <%= error %>
                        <button type="buttom" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
                <div class="text-center mb-4">
                    <h5>Register</h5>
                </div>
                <form action="/auth/register" method="POST">
                    <div class="mb-3">
                        <label for="username" class="form-label">Username:</label>
                        <input type="text" id="username" name="username" class="form-control" placeholder="Enter your username" required>
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password:</label>
                        <input type="password" id="password" name="password" class="form-control" placeholder="Enter your password" required>
                    </div>
                    <!-- hCaptcha Widget -->
                    <div class="mb-3">
                        <div class="h-captcha" data-sitekey="SECRET_KEY"></div>
                    </div>
                    <button type="submit" class="btn btn-warning w-100 text-white">Register</button>
                </form>
                <hr>
                <div class="text-center">
                    <p class="mb1">Already have an account?</p>
                    <a href="/auth/login" class="btn btn-secondary w-100">Sign In</a>
                </div>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
        <%- include('partials/footer') %>
    </body>
</html>
